{
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.883.0",
    "@aws-sdk/credential-providers": "^3.883.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node dist/cli/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}